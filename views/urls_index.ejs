<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Shrink Me - My Custom URLs</title>

  <!-- Bootstrap CSS -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js" type="text/javascript"></script>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
    integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS" crossorigin="anonymous">

    <!-- Navbar Script -->
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/js/bootstrap.bundle.min.js"
    type="text/javascript"></script>

  <!-- Additional CSS -->
  <style id="compiled-css" type="text/css">
    .masthead {
      height: 100vh;
      min-height: 500px;
      background-image: url('http://www.nicoleridout.com/img/tiles.jpg');
      background-size: cover;
      background-position: center;
    }
    .jumbotron {
      background: rgba(230, 230, 230, 0.90);
      padding: 6rem 2rem;
    }
    .container-e {
    width: 100%;
    justify-content: center;
    font-size: 20px;
    }
    .table td {
    padding: .75rem;
    border-top: 1px solid #dee2e6;
    }
    .btn {
      max-width: 100px;
    }
  </style>

</head>

<body>

  <!-- Navbar -->
  <% include partials/_header %>

  <!-- Sets the background and page display -->
  <header class="masthead">
    <div class="container-e h-100 ">
      <div class="jumbotron my-auto">

        <!-- Table that displays a list of the user's custom URLS -->
        <h1 class="display-4"><strong>My Links</strong> </h1>
        <br>
        <table class="table table-hover">
          <thead class="thead-light">
            <tr>
              <th scope="col">Original Link</th>
              <th scope="col">ShrinkMe Link</th>
              <th scope="col" class="text-right">Edit</th>
              <th scope="col" class="text-right">Delete</th>
            </tr>
          </thead>
          <tbody>
            <% for (shortURL in urls) { %>
            <tr>
              <td>
                <a href="<%= urls[shortURL] %>" target="_blank"><%= urls[shortURL] %></a>
              </td>
              <td>
                <a href="/u/<%= shortURL %>" target="_blank"><%= shortURL %></a>
              </td>
              <td>
                <a class="btn btn-block btn-light" style="float: right;" href="/urls/<%= shortURL %>" role="button">Edit</a>
              </td>
              <td>
                <form method="POST" action="/urls/<%=shortURL%>/delete">
                <button type="submit" class="btn btn-block btn-dark" style="float: right;">Delete</button>
                </form>
              </td>
            </tr>
            <% }; %>
          </tbody>
        </table>
      </div>
    </div>
  </header>
</body>

</html>
